# AGENTS.md

## 项目协作规则

- 核实中文文档、注释或 schema 是否乱码时，必须先使用 UTF-8 显式读取文件内容再判断；不要仅凭终端默认编码下的显示结果认定文件本身存在乱码。
- 数据库结构变更统一使用 Prisma Migration，不使用 `prisma db push` 修改正式开发结构。
- 修改登录流、页面结构、接口或数据库规则后，要同步检查相关文档是否需要更新。
- Git 提交信息使用 Conventional Commits 规范，类型用英文，描述用简洁中文。

## 参考入口

- 详细项目约定以 `CLAUDE.md` 为准。
